{% block hero %}
    {% if logo %}
        {% set image = asset('image/' ~ logo) %}
    {% else %}
        {% set image = hero.image %}
    {% endif %}
    <div class="hero-bg">
        <div class="hero-text">
            {% if hero.image %}
            <img alt="{{ hero.title }}" src="{{ image }}">
            {% endif %}
            <div>
                <h1>{{ hero.title }}</h1>
                <p>{{ hero.description }}</p>
            </div>
        </div>
    </div>
{% endblock %}
